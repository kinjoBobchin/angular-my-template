
      - run: ./hello.sh

workflows:
  version: 2
  build-test-deploy-except-master:
    jobs:
      - master_jobs:
          filters:
            branches:
              only: master
      - production_jobs:
          filters:
            branches:
              only: production
      - staging_jobs:
          filters:
            branches:
              only: staging
