version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.10.0
    steps:
      - checkout
      # - run: yarn install
      - run: echo "yarn install done"
      # - run: yarn build --prod
      - run:
        command: /hello.sh

